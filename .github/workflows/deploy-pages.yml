name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

env:
  DOTNET_VERSION: 10.0.x
  PUBLISH_DIR: bin/Release/net10.0/publish/wwwroot
  BASE_PATH: /CampaignTracker/

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Configure GitHub Pages base path
        run: sed -i 's#<base href="/" />#<base href="${{ env.BASE_PATH }}" />#' wwwroot/index.html

      - name: Publish app
        run: dotnet publish CampaignTracker.csproj -c Release

      - name: Create GitHub Pages fallback
        run: cp "${{ env.PUBLISH_DIR }}/index.html" "${{ env.PUBLISH_DIR }}/404.html"

      - name: Disable Jekyll
        run: touch ${{ env.PUBLISH_DIR }}/.nojekyll

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ${{ env.PUBLISH_DIR }}

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
