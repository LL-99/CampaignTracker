@page "/"
@using CampaignTracker.Model.Combats

@inject IDataService DataService

<PageTitle>Home</PageTitle>

<MudStack Spacing="4">
    <MudGrid Spacing="2">
        <MudItem xs="12" md="8">
            <MudStack Spacing="1">
                <MudText Typo="Typo.h4" HtmlTag="h1">Campaign Dashboard</MudText>
                <MudText Typo="Typo.body1" Color="Color.Secondary">
                    Track sessions, combats, player characters, and custom creatures from one MudBlazor workspace.
                </MudText>
            </MudStack>
        </MudItem>
        <MudItem xs="12" md="4" Class="d-flex justify-end">
            <MudButton Variant="Variant.Filled"
                       Color="Color.Primary"
                       StartIcon="@Icons.Material.Filled.Add"
                       OnClick="AddCombatButtonClicked">
                Add Combat
            </MudButton>
        </MudItem>
    </MudGrid>

    <MudGrid Spacing="3">
        <MudItem xs="12" sm="6" md="3">
            <MudPaper Elevation="1" Class="pa-4">
                <MudText Typo="Typo.overline" Color="Color.Secondary">Sessions</MudText>
                <MudText Typo="Typo.h4">@DataService.Campaign.Sessions.Count</MudText>
            </MudPaper>
        </MudItem>
        <MudItem xs="12" sm="6" md="3">
            <MudPaper Elevation="1" Class="pa-4">
                <MudText Typo="Typo.overline" Color="Color.Secondary">Combats</MudText>
                <MudText Typo="Typo.h4">@DataService.Campaign.Combats.Count</MudText>
            </MudPaper>
        </MudItem>
        <MudItem xs="12" sm="6" md="3">
            <MudPaper Elevation="1" Class="pa-4">
                <MudText Typo="Typo.overline" Color="Color.Secondary">Players</MudText>
                <MudText Typo="Typo.h4">@DataService.Campaign.PlayerCharacters.Count</MudText>
            </MudPaper>
        </MudItem>
        <MudItem xs="12" sm="6" md="3">
            <MudPaper Elevation="1" Class="pa-4">
                <MudText Typo="Typo.overline" Color="Color.Secondary">Creatures</MudText>
                <MudText Typo="Typo.h4">@DataService.Campaign.CustomCreatures.Count</MudText>
            </MudPaper>
        </MudItem>
    </MudGrid>

    <MudPaper Elevation="1">
        <MudToolBar Dense="true">
            <MudText Typo="Typo.h6">Campaign Data</MudText>
            <MudSpacer />
            <MudChip T="string" Color="Color.Primary" Variant="Variant.Outlined" Size="Size.Small">
                JSON Preview
            </MudChip>
        </MudToolBar>
        <MudDivider />
        <MudText Typo="Typo.body2" Class="pa-4">
            @DataService.GetCampaignString()
        </MudText>
    </MudPaper>
</MudStack>


@code {
    private void AddCombatButtonClicked()
    {
        _ = new Combat(DataService.Campaign);
    }
}
